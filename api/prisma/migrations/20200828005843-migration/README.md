# Migration `20200828005843-migration`

This migration has been generated by nampdn at 8/28/2020, 7:58:43 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `ekklisia_dev`.`DraftProfile` ADD COLUMN `createdAt` datetime(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3);

ALTER TABLE `ekklisia_dev`.`Activity` RENAME INDEX `Activity.slug` TO `Activity.slug_unique`

ALTER TABLE `ekklisia_dev`.`Attendance` RENAME INDEX `Attendance.slug` TO `Attendance.slug_unique`

ALTER TABLE `ekklisia_dev`.`Group` RENAME INDEX `Group.slug` TO `Group.slug_unique`

ALTER TABLE `ekklisia_dev`.`Profile` RENAME INDEX `Profile.oldId` TO `Profile.oldId_unique`

ALTER TABLE `ekklisia_dev`.`Profile` RENAME INDEX `Profile.slug` TO `Profile.slug_unique`

ALTER TABLE `ekklisia_dev`.`User` RENAME INDEX `User.email` TO `User.email_unique`
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200816073016-migration..20200828005843-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
@@ -18,12 +18,14 @@
   profileId  String
 }
 model Meta {
-  id    String @default(cuid()) @id
-  key   String
-  value String
-  type  String
+  id             String        @default(cuid()) @id
+  key            String
+  value          String
+  type           String
+  DraftProfile   DraftProfile? @relation(fields: [draftProfileId], references: [id])
+  draftProfileId String?
 }
 model DraftProfile {
   id          String    @default(cuid()) @id
@@ -31,8 +33,9 @@
   nationalId  String?
   phoneNumber String?
   birthday    DateTime?
   meta        Meta[]
+  createdAt   DateTime  @default(now())
 }
 model Profile {
   id           String            @default(cuid()) @id
```


